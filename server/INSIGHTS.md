# Server — Insights

> Running log of gotchas & lessons learned for `@devdigest/api` (Fastify 5 + Drizzle/Postgres), including `src/modules/repo-intel`.
> Maintained by the `engineering-insights` skill. APPEND-ONLY — add entries, never edit or delete existing ones.

## What Works
<!-- Approaches/solutions that worked -->
- `@fastify/multipart` v10 works with Fastify v5. Register it INSIDE a route plugin (not globally) to scope it; `req.file()` is then available within that plugin scope. Always call `data.toBuffer()` before any throwing validation to avoid "Request is aborted" errors from unconsumed streams.
- Drizzle `jsonb('col').$type<string[]>()` infers the insert type as `string[] | null | undefined` — pass the value directly (no `as object` cast). Casting to `object` is rejected by Drizzle's overloaded insert signatures.
- `adm-zip` v0.5.17 ships its own `@types/adm-zip` is still needed separately (it does NOT ship built-in `.d.ts` at this version). Install `pnpm add -D @types/adm-zip`.

## What Doesn't Work
<!-- Dead ends & antipatterns -->
- Block comments (`/* ... */` and `/** ... */`) that contain `*/` inside a string literal (e.g. `'**/*.it.test.ts'`) prematurely close the comment — TypeScript reports baffling "Expression expected" / "Unterminated string literal" errors on the NEXT non-comment line. Use line comments (`//`) or escape the `*/` sequence in JSDoc copy.
- Drizzle `jsonb` insert: do NOT cast `string[] | null` to `object | null` — Drizzle's overloaded `.values()` doesn't accept the generic `object` type and will error with a confusing "no overload matches" message. Pass the typed value directly.

## Codebase Patterns
<!-- Conventions, architectural decisions -->
- `reviewer-core` already returns generation cost: `reviewPullRequest()` → `ReviewOutcome.costUsd` (real provider `usage.cost` when present, else price-book estimate). It is computed for free on every run — don't recompute cost server-side; capture `outcome.costUsd` in `run-executor.ts`. (Before the run-cost feature, the executor destructured only `{ tokensIn, tokensOut, grounding }` and silently dropped it.)
- PR-list aggregate columns (score, cost) are computed ON-READ in the `GET /pulls` handler (`modules/pulls/routes.ts`), NOT stored denormalized: one `inArray(prId, prIds)` query over the source table + a JS `Map` grouping pass, then merged into each row. Follow this same shape for any new per-PR aggregate.
- There is no review-session / batch id in the schema. "Cost of the latest review" is approximated by time-clustering `agent_runs` (newest-first, `status='done'`, gap ≤ `BATCH_GAP_MS` = 90s). Replace with exact grouping if a `review_session_id` column is ever added.

## Tool & Library Notes
<!-- Dependency quirks -->

## Recurring Errors & Fixes
<!-- Repeated error + the fix -->
- Adding a NON-optional field to an `agent_runs` write means updating EVERY `completeAgentRun` call site, not just the happy path: `run-executor.ts` has the success path PLUS `failAll` and failure/cancel branches and the `traceFromBuffer` synthetic stats, AND the wrapper signature in `modules/reviews/repository.ts`. Miss one → typecheck fails (or a branch writes a row missing the field). Grep `completeAgentRun` before assuming you've got them all.
- `pnpm db:generate` (drizzle-kit v0.30.6) uses a TUI that never selects a default when stdin is not a real TTY (piped, `yes`, `socat`, `CI=true` all fail — it exits code 0 with no migration generated). Workaround when a DROP+ADD triggers the rename-detection prompt: split into two migrations — (1) add-only migration (no drops → no rename prompt), (2) drop-only migration (no adds → no rename prompt). Each runs without interaction. Set `strict: false` in `drizzle.config.ts` during generation if needed, then restore it.

## Session Notes
<!-- Datestamped session summaries -->
- 2026-06-21 — Re-introduced per-run cost (USD). Added nullable `costUsd` (`cost_usd` double precision) to `agent_runs` via generated migration `0010_chemical_skreet.sql` (`pnpm db:generate` then `pnpm db:migrate` — migrations are NOT applied on boot). Captured `outcome.costUsd` in `run-executor.ts`, persisted in `run.repo.completeAgentRun`, surfaced on `RunStats`/`RunSummary`/`PrMeta` contracts (`vendor/shared`), summed latest-batch per PR in `GET /pulls`. `cost_usd` is nullable everywhere so missing data stays distinct from a real $0.
- 2026-06-28 — Implemented full Skills feature server-side (L02). New module `modules/skills/` (routes/service/repository/helpers/constants + unit tests). Extended `POST /agents/:id/skills` with bulk `{ skills: [{skill_id, order, enabled}] }` form; added `PATCH /agents/:id/skills/:skillId { enabled }` toggle. Import pipeline: `@fastify/multipart` v10 + `adm-zip` for zip extraction. Wired skills into `run-executor.ts`: `linkedSkills` join already returns `skill.enabled`, filter on both flags, inject bodies via `{ skills: skillBodies }`. `container.skillsRepo` added. Typecheck + all 124 unit tests green.
- 2026-06-28 — Conventions feature foundation (L0x). New tables `convention_scans` + extended `conventions` (dropped `accepted`, added `status/evidence_line/evidence_url/skill_id/scan_id/created_at`). New module `modules/conventions/` (constants.ts, helpers.ts, repository.ts + unit tests). `container.conventionsRepo` added. contracts/knowledge.ts updated (both server+client copies). Migrations 0012 + 0013 generated via two-step approach (see Recurring Errors below). Typecheck clean + 156 unit tests green.

## Recurring Errors & Fixes (continued)
- `LLMProvider.completeStructured` takes `messages: ChatMessage[]`, NOT separate `system`/`prompt` fields. Pass `[{ role: 'system', content: SYSTEM }, { role: 'user', content: userMessage }]`. TypeScript infers `T` as `unknown` unless you explicitly annotate the generic: `llm.completeStructured<MyType>({ schema: MyZodSchema, ... })`. Import both the Zod schema value and the TS type alias: `import { MySchema } from '...'` + `import type { MySchemaType } from '...'` (or alias one).
- Zod `.default([])` (or `.default(value)` on any type) makes the field **required in the TypeScript output type** (`z.infer<...>`), NOT optional. Any code that manually constructs an object conforming to that type without including the new field will fail typecheck — even though the field has a default for parsing. When you add a `.default(...)` field to a vendored contract (e.g. `Intent`), grep for manual object constructions of that type (`grep -rn "in_scope.*out_of_scope"` pattern) and add the field there too. The pre-scaffolded `getIntent` in `server/src/modules/reviews/repository/pull.repo.ts` was one such place — it constructs `Intent` manually from DB rows, not via `.parse()`.
- The Edit tool may silently convert ASCII single quotes (U+0027) to Unicode curly quotes (U+2018/U+2019) when the matched `old_string` block contains other Unicode characters (e.g. `·` U+00B7 or smart apostrophe U+2019). TypeScript rejects curly quotes as string delimiters and emits TS1127 "Invalid character" on every quote. Detection: `sed -n 'Np' file | cat -A` reveals `M-bM-^@M-^X` / `M-bM-^@M-^Y`. Fix: use a Python script to do a precise string replacement with explicit byte values rather than re-running Edit on the affected lines.

## Session Notes (continued)
- 2026-06-28 — Conventions service track (Lxx). Added `CONVENTIONS_SYSTEM` to constants.ts; created `extractor.ts` (pure extraction pipeline: messages build → LLM structured call → confidence gate → file-presence gate → evidence verification gate → cap at MAX_CANDIDATES); `service.ts` (extract/view/judge/judgeBulk/createSkill — reads config+sample files via git.readFile, delegates to extractor, persists via conventionsRepo, materialises skills via SkillsService.create); `routes.ts` (5 routes: POST extract, GET, PATCH :candidateId, POST judge, POST skill); registered in modules/index.ts; seeded 1 scan + 3 candidates in seed.ts; 7 new extractor.test.ts tests. 163 unit tests green, typecheck clean.

- 2026-06-30 — Intent Layer Unit 1 (shared contracts). Added `risk_areas: z.array(z.string()).default([])` to `Intent` in `vendor/shared/contracts/brief.ts`; added `intent: z.string().nullish()` to `PromptAssembly` in `trace.ts`; updated `FEATURE_MODELS[review_intent]` default to `openrouter`/`google/gemini-2.5-flash-lite` in `platform.ts`; added optional `requireParameters?: boolean` to `StructuredRequest` in `adapters.ts`. Mirrored all four byte-identically to `client/src/vendor/shared/`; updated `client/src/lib/feature-models.ts`. Also patched `modules/reviews/repository/pull.repo.ts` line 67 (pre-scaffolded `getIntent` returned `Intent` without `risk_areas` — added `risk_areas: []` as minimal fix; Unit 2 must update to `row.riskAreas ?? []` after the migration adds the DB column). Breaking-change: none (additive). Response-schema: additive only (new nullish/defaulted fields). Typecheck green in server (163 tests) + client (58 tests).

- 2026-06-30 — Intent Layer Unit 2 (migration 0014). Added `riskAreas: jsonb('risk_areas').$type<string[]>().notNull().default(sql\`'[]'::jsonb\`)` and `headSha: text('head_sha')` (nullable) to `prIntent` in `server/src/db/schema/reviews.ts`. `pnpm db:generate` (drizzle-kit v0.30.6) ran cleanly without an interactive rename prompt — confirmed that an **add-only** schema change (no drops, no renames) never triggers the rename-detection TUI even under non-TTY. Generated `0014_lean_gargoyle.sql`: two `ALTER TABLE "pr_intent" ADD COLUMN` statements. Updated `pull.repo.ts` `getIntent` return from `risk_areas: []` stopgap to `risk_areas: row.riskAreas ?? []` (reads the real column now that the migration adds it). Typecheck + 163 unit tests green.

- 2026-06-30 — Intent Layer Unit 4a (spec/plan resolver adapter). Created `server/src/adapters/specfetch/` (5 files) + extended `platform/config.ts` with `intentSpecAllowlist` and `intentSpecMaxTokens`. SSRF guard implemented purely with Node.js builtins (`node:dns/promises` for DNS resolution; `fetch` with `redirect: 'manual'` for hop-by-hop redirect re-validation; private IP range table as plain number ranges to avoid 32-bit signed-integer overflow from bitwise ops). Allowlist supports wildcard subdomain entries (`*.notion.so`). JS bitwise ops truncate to 32-bit signed int — converting IP quads via multiplication (`parts[0]! * 16777216 + ...`) avoids the sign-bit issue that `<<` operators introduce for addresses ≥ 128.x.x.x. All 163 unit tests green; typecheck clean.

## What Works
<!-- Approaches/solutions that worked -->
- SSRF IP-range checking without any npm library: convert IPv4 to a number via multiplication (`a*16777216 + b*65536 + c*256 + d`) and compare against a `{ start, end }` table. Avoids the signed-32-bit overflow that bitwise-shift operators introduce (e.g. `224.0.0.0 = 0xe0000000` becomes negative when shifted).
- `node:dns/promises` `lookup(hostname, { all: true })` resolves ALL A/AAAA records; checking every address guards against DNS-rebinding where one record is public and a subsequent lookup might return a private IP.
- `fetch` with `redirect: 'manual'` gives control over each redirect hop so each can be re-validated against the allowlist + IP blocklist before following.

## Session Notes (continued)
- 2026-06-30 — Intent Layer Unit 4 (backend intent module). Created `server/src/modules/intent/` (repository.ts, constants.ts, service.ts, routes.ts). Added `get intentRepo()` + `get specResolver()` to `platform/container.ts`; both added to `ContainerOverrides`. Added `MockSpecResolver` to `adapters/mocks.ts`. Registered `intent` in `modules/index.ts`. Key patterns: (1) `specResolver` getter uses a closure-based lazy wrapper to avoid async getter, since `SpecFetchResolver` needs `GitHubClient` which resolves asynchronously via `container.github()`; the inner `SpecFetchResolver` is cached in the closure after first `resolve()` call. (2) `Intent.risk_areas` has `.default([])` → the Zod input type has `risk_areas?: string[] | undefined` while the output type has `risk_areas: string[]`; `ZodType<T>` requires both to be T, so `schema: Intent as z.ZodType<Intent>` is the required cast — standard Zod v3 workaround for .default() fields. (3) Logger interface defined locally in service.ts as `{ info(obj: unknown, msg?: string): void; warn(obj: unknown, msg?: string): void }` to avoid cross-module coupling with run-executor. Typecheck clean + 163 unit tests green.

## Recurring Errors & Fixes (continued)
- `ZodType<T>` generic mismatch with Zod `.default()` fields: when a Zod schema has `.default(value)` on any field, the _input_ type makes that field optional (`field?: T | undefined`) while the _output_ (inferred) type has it required (`field: T`). Calling `completeStructured<Out>({ schema: MySchema })` fails because `ZodType<Out>` requires _input_ = _output_ = `Out`. Fix: cast `schema: MySchema as z.ZodType<Out>`. This is a structural cast only — the parsed `.data` is still fully typed as `Out`.

- 2026-06-30 — Intent Layer Unit 5 (run-executor wiring). Added `IntentService` call to `run-executor.ts`: (1) `formatIntent(PrIntentRecord)` module-level helper compacts the record into the prompt string (omits empty lists); (2) intent derived ONCE in `executeRuns` via `runLog.step('Deriving PR intent', ...)` using `{ kind: 'tool' }` so it appears amber in the Live Log — same pattern as the diff load; (3) failure is non-fatal (try/catch around the step call — `runLog.step` already emits the error event before rethrowing, outer catch just logs an info and leaves `intentText` undefined); (4) `intent?: string` added to `runOneAgent` signature so the already-derived string threads into `reviewPullRequest` without a second derivation; (5) `...(intent ? { intent } : {})` spread in `reviewPullRequest` call mirrors the `prDescription` pattern — when absent the call is byte-identical to pre-feature. `run-executor.ts`'s `Logger` type (`{ info, warn, error, debug }`) is structurally compatible with `IntentService.Logger` (`{ info, warn }`) so the logger passes through without type friction. Typecheck clean + 163 unit tests green.

## Session Notes (continued)
- 2026-07-01 — Intent Layer test-writer pass (Units 4a + 4 hermetic tests). Wrote `server/src/adapters/specfetch/specfetch.test.ts` (31 tests) and `server/src/modules/intent/service.test.ts` (5 tests). Key lessons: (1) `vi.mock('node:dns/promises', ...)` is Vitest-hoisted before all static imports — `web-source.ts` receives the mocked `lookup` on its own import, so DNS resolution is fully controlled per-test without modifying production code. (2) `MockLLMProvider` constructor only accepts `'openai' | 'anthropic'` for the `id` param — do NOT pass `'openrouter'` even when the service internally resolves to that provider. (3) The web-source.ts size cap (`MAX_RESPONSE_BYTES = 512 KB`) fires AFTER `chunks.push(value)`, so a single-chunk response of N bytes > cap will still produce an N-byte result. To exercise the cap properly, deliver the body via a multi-chunk `ReadableStream` (e.g. 3 × 256 KB pulls; the loop stops after chunk 2 reaches exactly 512 KB). (4) `IntentService` depends on `Container` directly — hermetic service tests bypass the DI class via `as unknown as Container` with a plain-object mock; the only DB call that needs mocking is `resolveFeatureModel`'s `db.select().from(t.settings).where()` chain (return `[]` → falls back to registry default). (5) All 199 hermetic tests remain green; `pnpm typecheck` clean.

## What Doesn't Work
<!-- Dead ends & antipatterns -->
- Do NOT use `new Response(largeString)` to test the 512 KB size cap in web-source.ts. Node.js 22 delivers the entire body as ONE chunk, so the cap check fires after accumulating the full body — the result is still the original string size (not capped). Use a `ReadableStream` with `pull()` callbacks that enqueue fixed-size chunks; see the streaming-cap test in `specfetch.test.ts` for the exact pattern.

## Open Questions
<!-- Still unresolved -->
