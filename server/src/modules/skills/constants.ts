/** Constants for the skills module. */

/** Initial body version recorded for a newly-created skill. */
export const INITIAL_SKILL_VERSION = 1;

/** Change note written for the initial version snapshot on insert. */
export const INITIAL_SKILL_NOTE = 'Initial';

/** Default skill type applied during file import when no type is specified. */
export const DEFAULT_SKILL_TYPE = 'custom' as const;
